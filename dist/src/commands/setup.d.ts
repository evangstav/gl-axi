export declare const SETUP_HELP = "usage: gl-axi setup hooks\nInstalls the AXI session-start hook so agents get gl-axi's ambient context.";
export declare function setupCommand(args: string[]): Promise<string>;
